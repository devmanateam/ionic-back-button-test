import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'capacitor3-backbutton-test',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
